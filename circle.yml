test:
  override:
    - coverage run vaapp/app/manage.py test
  post:
    - coverage html -d $CIRCLE_ARTIFACTS
deployment:
  staging:
    branch: master
    codedeploy:
      app:
        deployment_group: staging-instance-group
