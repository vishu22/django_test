---
test:
  override:
    - coverage run manage.py test
  post:
    - coverage html -d $CIRCLE_ARTIFACTS
deployment:
  staging:
    branch: development
    codedeploy:
      django_test:
        deployment_group: staging-instance-group

